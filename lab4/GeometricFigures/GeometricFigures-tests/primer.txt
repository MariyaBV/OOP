class ClassA
{
public:
    ~ClassA() {delete this}
    ClassA() {}
};

class ClassB : public ClassA
{
public:
    ~ClassB() {delete this}
    ClassB() : ClassA() {}
};

int main()
{
    ClassA * pA = new ClassB();
    delete pA; // delete only class A, class B not delted
};

//если базовый класс предназначен дл€ полиморфного использовани€, 
то его деструктор должен объ€вл€тьс€ виртуальным. 
ƒл€ реализации механизма виртуальных функций каждый 
объект класса хранит указатель на таблицу виртуальных функций vptr, 
что увеличивает его общий размер. ќбычно, 
при объ€влении виртуального деструктора такой класс уже имеет 
виртуальные функции, и увеличени€ размера 
соответствующего объекта не происходит.

≈сли же базовый класс не предназначен дл€ полиморфного использовани€ 
(не содержит виртуальных функций), то его деструктор не должен 
объ€вл€тьс€ виртуальным.